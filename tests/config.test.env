# Test configuration template.
# TEST_ROOT must be set before this file is sourced/evaluated.
# The test runner substitutes TEST_ROOT and writes the result to config.env.

WORKING_DIR=__TEST_WORKING_DIR__
DEFAULT_BUILDER=ubuntu_amd64
DEFAULT_STAGE=__TEST_STAGING_DIR__
GITHUB_REPO=https://github.com/mock/repo/releases/download
SIG_PASS=test-passphrase
INDEX_DIR=metadata
INDEX_FILE=index.json
PUB_KEY1=ci.pub
PUBLISH_BRANCH=main
